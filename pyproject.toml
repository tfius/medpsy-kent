[project]
name = "medpsy-eval"
version = "0.1.0"
description = "Eval harness for the medpsy local model served via LM Studio"
requires-python = ">=3.10"
dependencies = [
    "httpx>=0.27",
]

[tool.uv]
package = false
